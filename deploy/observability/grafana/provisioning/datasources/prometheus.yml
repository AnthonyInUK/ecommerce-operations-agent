# 自动配置 Prometheus 数据源，Grafana 启动即可用。
apiVersion: 1
datasources:
  - name: Prometheus
    uid: aa-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
